kind: Service
apiVersion: v1
metadata:
  name: SERVICENAME
  namespace: NAMESPACE
spec:
  type: LoadBalancer
  loadBalancerIP: PUBLIC_IP
  ports:
  - name: frontend-webui
    port: 80
    targetPort: frontend-cport
    protocol: TCP
  selector:
    app: SERVICENAME
